/**
 * Centralized virtualization tokens — single source for row heights, thresholds, and overscan.
 */

export const VIRTUAL_PICKER = {
  OPTION_HEIGHT: 32,
  OVERSCAN: 10,
  THRESHOLD: 150,
} as const;
